# Project Rules

- Do not commit secrets or .env files.
- Treat config/contracts.json as generated output; do not commit unless asked.
- Update config/symbols.list first, then rebuild config/symbols.yaml.
- Prefer existing make targets and CLI commands for common tasks.
