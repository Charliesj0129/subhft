"""Faithful offline backtest of the Zeiierman 'Machine Learning RSI' KNN strategy.

Standalone / additive research artifact (does NOT touch production, frozen
detectors, or the parallel Codex-dirty tracked files). Offline metrics only;
float math is allowed here per .agent rule 25 (research artifacts).
"""
