This folder mirrors the legacy top-level `reports/` artifacts.

Migration policy:
- New research reports should prefer `research/knowledge/reports/`.
- Legacy tools that still write to top-level `reports/` remain supported.
