# strategies

## Purpose
Built-in strategy implementations.

## Key Files
- `src/hft_platform/strategies/simple_mm.py`: Example market making strategy.

## Usage
- Configure via `config/base/main.yaml` or CLI overrides.
- Use as reference when creating new strategies.

## Extension Points
- Add new strategy modules here.
- Register in settings or via CLI `--strategy-module`.
