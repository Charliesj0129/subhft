# Project Rules

- Do not commit secrets or .env files.
- Treat config/contracts.json as generated output.
- Use config/symbols.list as the source of truth.
- Regenerate config/symbols.yaml after list changes.
- Prefer make targets and CLI commands.
