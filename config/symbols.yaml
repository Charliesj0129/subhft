# Minimal subscription list for contract-refresh verification session
# (original 357-entry version backed up as symbols.yaml.bak.*)
# Uses R1 continuous aliases so the contract refresh thread keeps the
# concrete month code current across rollovers without YAML edits.
symbols:
- code: TXFR1
  exchange: TAIFEX
  group: 0
- code: TMFR1
  exchange: TAIFEX
  group: 0
