# Phase 2 Shadow Trading: real feed, strategy-driven signals, no real orders
# Usage: HFT_MODE=sim HFT_ORDER_SHADOW_MODE=1 HFT_SYMBOLS=TXFD6,TMFD6

mode: sim

shadow:
  enabled: true

shioaji:
  activate_ca: false

# Multi-strategy shadow: strategies loaded from config/base/strategies.yaml
# OpportunisticMM on TXF + CBS on TMF

symbols:
  - "TXFR1"    # R1 = Shioaji continuous front-month (no monthly update)
  - "TMFR1"
